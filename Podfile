# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Drop Memory' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Drop Memory
    pod 'Firebase/Core'

  target 'Drop MemoryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Drop MemoryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
